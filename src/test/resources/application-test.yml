server:
  servlet:
    encoding:
      force: true

spring:
  jackson:
    serialization:
      fail-on-empty-beans: false
  datasource:
    url: "jdbc:postgresql://localhost:5432/bookshoptest"
    username: "postgres"
    password: "TheWitcher3"
  jpa:
    show-sql: false
    open-in-view: false
    generate-ddl: true
    hibernate:
      ddl-auto: validate
  freemarker:
    expose-session-attributes: true

upload:
  path: "C:/Users/_ADMIN_/IdeaProjects/BookShop"